import React from "react";
import styles from './Header.module.css';

const Head = () => {
    return(
        <header className={styles.header}>
            hello
        </header>
    )
}

export default Head;
